import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "StudoCu Document Downloader - Download PDF Documents",
  description: "Download StudoCu documents as PDF files. Fast, reliable, and easy to use document downloader tool.",
  keywords: ["studocu", "download", "pdf", "document", "converter"],
  openGraph: {
    title: "StudoCu Document Downloader",
    description: "Download StudoCu documents as PDF files",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
